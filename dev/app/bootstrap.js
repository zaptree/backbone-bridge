/**
 * Here You can add code that will run before your program begins plus pre-load all the modules that you want
 */
require([
	'underscore'
	//put all your module controllers (and any thing that is required by your application in here for the build process)
	//,'modules/layout/layout'
	//,'modules/blog/blog'
], function   (_) {
	return function(app){
		// run any global code in here
	}
});